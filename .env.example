# ============================================================
# Heliograph — Environment Configuration
# ============================================================
# This file contains secrets and environment-specific settings.
# Application config (models, agents, RAG, domain) -> config.yaml
# ============================================================

# === API Configuration ===
# OpenAI-compatible API endpoint (e.g., OpenAI, Mistral, vLLM, Ollama, LiteLLM)
API_BASE_URL=https://api.example.com/v1
API_KEY=your-api-key

# === Model overrides (optional — config.yaml takes priority) ===
# Uncomment to override config.yaml model settings via environment:
# MODEL_HEAVY=mistralai/Mistral-Small-3.2-24B-Instruct-2506
# MODEL_CODE=mistralai/Devstral-Small-2-24B-Instruct-2512
# MODEL_LIGHT=mistralai/Ministral-3-8B-Instruct-2512
# MODEL_REASONING=mistralai/Ministral-3-14B-Reasoning-2512
# MODEL_EMBED=BAAI/bge-m3
# MODEL_RERANK=BAAI/bge-reranker-large

# === Retry settings (for 502/503/429) ===
RETRY_MAX_ATTEMPTS=8
RETRY_BASE_DELAY=2.0
RETRY_MAX_DELAY=120.0

# === Workspace ===
# For local dev:
WORKSPACE_PATH=./workspace
# For Docker: path on the HOST machine to mount
HOST_WORKSPACE_PATH=./workspace

# === Docker deployment ===
# Image to use (set by CI or manually)
DOCKER_IMAGE=your-registry.example.com/heliograph:latest
# Web UI port on host
WEB_PORT=8080
OPENWEBUI_PORT=3000
OPENWEBUI_SECRET_KEY=your-openwebui-secret-key
# Indexer interval in seconds (3600 = 1 hour)
INDEXER_INTERVAL_SECONDS=3600

# === Host paths for bind mounts ===
# All paths are relative to docker-compose.yml location.
# The .vectordb built locally is reused directly by containers.
HOST_VECTORDB_PATH=./.vectordb
HOST_GRAPHDB_PATH=./.graphdb
HOST_CONTEXT_PATH=./context
HOST_AGENTS_PATH=./agents
HOST_REPORTS_PATH=./reports
HOST_OUTPUT_PATH=./output
HOST_WEBLOGS_PATH=./web/logs
HOST_PROJECTS_PATH=./projects
